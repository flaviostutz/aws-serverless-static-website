name: sls-package

on:
  push:
    branches:
      - main

jobs:
  sls-package:
    uses: flaviostutz/gh-workflows-serverless/sls-package.yml@main
    with:
      stage: pr${{ github.event.number }}
      workdir: ./
